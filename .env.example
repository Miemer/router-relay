# ── Inbound auth: tokens clients send as `Authorization: Bearer <token>` ──
# Comma-separated. Empty = OPEN relay (dev only — never in production).
RELAY_API_KEYS=sk-relay-dev-change-me

# ── Upstream OpenAI-compatible provider ──
UPSTREAM_BASE_URL=https://api.openai.com/v1
UPSTREAM_API_KEY=sk-your-openai-key
# UPSTREAM_BASE_URL=https://openrouter.ai/api/v1
# UPSTREAM_API_KEY=sk-or-...
UPSTREAM_ORGANIZATION=
DEFAULT_MODEL=

# ── Server ──
LISTEN_HOST=127.0.0.1
LISTEN_PORT=8787
UPSTREAM_TIMEOUT=600
LOG_LEVEL=info

# ── P1 Router ──
ROUTER_ENABLED=false
ROUTER_OBSERVE_ONLY=false
ROUTER_TIMEOUT_SECONDS=2.0
ROUTER_STICKY_TURNS=3
ROUTER_CONFIDENCE_THRESHOLD=0.55
ROUTER_LARGE_CONTEXT_CHARS=64000
ROUTER_DECISION_DB=
# P3-prep: directory for date-partitioned JSONL capture
# (router-samples-YYYY-MM-DD.jsonl). Empty = no capture.
ROUTER_CAPTURE_DIR=
ROUTER_LOG_DECISIONS=true
# Tier → model mapping as JSON. Empty = built-in preset for marketingforce
# (c0=qwen3-max, c1=deepseek-r1, c2=gpt-5.4, c3=claude-opus-4-8).
# ROUTER_TIERS={"c3":{"model":"claude-opus-4-8"}}
ROUTER_TIERS=

# ── P2 Ensemble (B5 fusion) ──
ENSEMBLE_ENABLED=false
ENSEMBLE_PROPOSERS=
ENSEMBLE_AGGREGATOR=
ENSEMBLE_MIN_TIER=c2
ENSEMBLE_MIN_SUCCESSFUL=2
ENSEMBLE_PROPOSER_TIMEOUT=60
ENSEMBLE_AGGREGATOR_TIMEOUT=120
ENSEMBLE_CANDIDATE_MAX_CHARS=24000
